# 고객앱 예약 기능 동기화 작업 기록

## 목표

- 고객앱 `apps/customer/src/app/mypage/direct-booking`을 기능 기준으로 삼는다.
- 홈페이지 `/booking`은 목록값, 필터, 입력 순서, 검증, 저장 흐름을 동일하게 제공한다.
- 시각 표현은 Stay Halong 홈페이지 디자인 체계를 유지한다.

## 제약

- `sht-platform` 구현과 플랫폼 DB 스키마·RLS·함수·결제 엔드포인트는 변경하지 않는다.
- 고객 식별, 예약, 견적, 결제의 기준 원장은 플랫폼 Supabase를 유지한다.
- 홈페이지 DB `homepage_booking_carts`를 로그인 고객의 공식 장바구니 초안 저장소로 사용한다.
- 브라우저 로컬 장바구니는 홈페이지 DB 동기화를 위한 임시 사본으로만 유지한다.
- 홈페이지 장바구니 가격은 참고값이며 최종 저장 직전에 플랫폼 데이터로 재검증한다.
- 결제 기능과 기존 플랫폼 진입점은 병렬 운영 승인 전까지 유지한다.

## 현재 조사 방향

- 고객앱의 서비스별 페이지와 공유 유틸리티를 읽어 입력 옵션과 단계 순서를 추출한다.
- 홈페이지의 `src/app/booking`과 `src/lib/booking-*` 구현을 동일 항목으로 대조한다.

## 추가 확정 사항

- 사용자가 홈페이지의 저장 결과도 고객앱과 동일해야 한다고 명시했다.
- 서비스 선택은 먼저 홈페이지 DB 장바구니에 저장한다.
- 장바구니의 최종 예약 저장 시 플랫폼 `quote`, `reservation`, 서비스별 상세 테이블에 고객앱과 같은 계약으로 변환 저장한다.
- 기존 플랫폼 코드는 수정하지 않으며 홈페이지에서 로그인 사용자의 플랫폼 세션과 기존 RLS를 그대로 사용한다.

## 구현 결과

- `/booking`의 8개 서비스를 고객앱 순서로 배치하고 플랫폼의 실시간 상품·요금·견적 데이터를 조회한다.
- 각 서비스 폼은 고객앱의 연속 필터와 저장에 필요한 코드를 `metadata.platform` 계약으로 홈페이지 장바구니에 보관한다.
- `/api/booking/submit`은 홈페이지 DB의 공식 장바구니만 읽고, 플랫폼 세션·RLS로 사용자 소유권을 확인한다.
- 최종 저장 시 플랫폼 요금과 활성 상태를 재검증한 뒤 `quote`, `reservation`, 서비스별 상세 테이블에 저장한다.
- 일부 항목 저장 실패 시 이미 생성한 부모 예약을 롤백하고, 모두 성공한 경우에만 `updated_at` 낙관적 잠금으로 홈페이지 장바구니를 비운다.
- 크루즈는 요금 RPC·휴일 할증·프로모션 청구를, 패키지는 하위 서비스·스하 좌석 요금을, 티켓은 메뉴·셔틀·요금 세부 계약을 고객앱 기준으로 반영했다.

## 검증 결과

- 1440px 데스크톱과 390px 모바일에서 서비스 순서·반응형·가로 넘침을 확인했다.
- 8개 서비스 페이지와 공항 연속 필터, 패키지 하위 서비스 로드, 홈페이지 DB 장바구니 표시를 확인했다.
- 인증 없는 최종 저장 API가 401로 차단됨을 확인했다.
- `npm run lint`, `npm run build`, `git diff --check`를 통과했다. 린트의 기존 관리자 이미지 경고 2건은 이 작업 범위 밖이다.
- 플랫폼의 실제 예약 INSERT는 운영 유사 데이터 오염을 피하기 위해 자동 검증에서 실행하지 않았다.

## OnePay 고객 결제 승인

- 홈페이지 서버가 플랫폼 서비스 키로 고객 소유 예약과 금액을 재검증한 후 `reservation_payment` 대기 레코드를 생성한다.
- 여러 예약의 결제는 같은 서버 생성 묶음 식별자를 `memo`에 저장해 OnePay 단일 거래 콜백에서 함께 완료 처리한다.
- OnePay 요청·복귀·알림은 홈페이지 서버의 환경 변수로만 서명하며, 브라우저에는 OnePay 비밀 값을 전달하지 않는다.
- 결제 시작 실패 후에도 새 예약을 중복 생성하지 않도록, 체크아웃 완료 화면에서 같은 예약 ID와 멱등 키로 결제를 재시도한다.

## 여행 가이드 크루즈 예산

- 예산 선택지는 현재 공개된 크루즈의 VND 성인 1인 요금을 선택한 성인 인원과 객실 수로 환산해 만든다.
- 추천 엔진도 같은 성인 인원·객실 수 기준으로 예산을 비교하며, 아동·유아 요금과 추가 옵션은 최종 확인 대상으로 안내한다.

## 여행 가이드 운영 문서

- 서비스별 입력, 실제 필터, 현재 수집만 하는 상담 참고값, 카탈로그 설정 위치와 개선 순서를 `docs/travel-guide-recommendation-plan.md`에 기록했다.
- 크루즈 전용으로 추천 뷰 필드별 실제 사용 여부, 점수, 추가 고객 선택 항목과 데이터 정규화 순서를 문서화했다.
- 크루즈 추천은 이제 `cruise_tags_v2`의 관리자 활성 태그와 근거를 직접 조회해, 고객 선호가 일치하는 후보에만 가점과 근거 문구를 적용한다.
